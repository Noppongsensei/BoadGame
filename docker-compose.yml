version: '3.8'

services:
  # app service temporarily commented out until we fix build issues
  # app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     - postgres
  #   environment:
  #     - DB_HOST=postgres
  #     - DB_USER=postgres
  #     - DB_PASSWORD=postgres
  #     - DB_NAME=avalon
  #     - DB_PORT=5432
  #   volumes:
  #     - .:/app
  #   restart: unless-stopped
  #   networks:
  #     - avalon-network

  postgres:
    image: postgres:14-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=avalon
    volumes:
      - postgres-data:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      - avalon-network

volumes:
  postgres-data:

networks:
  avalon-network:
    driver: bridge
